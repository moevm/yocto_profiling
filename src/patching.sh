#! /bin/bash

SOURCE_DIR=$(dirname $0)
POKY_DIR=$SOURCE_DIR/poky
PATCHES_PATH=$SOURCE_DIR/yocto_patches

# INPUT
# GET PATCHES LIST FROM USER

# python3 $PATHCHES_PATH/main.py --poky-path $POKY_DIR --dir-path $PATCHES_PATH --patch ... --patch ...

