To build docker use `docker build -t my-http-server .`
To start container use `./run.sh <port>`
To start many containers use `./start_servers.sh <start port> <num servers>` 
To stop and rm all running containers use `./rm_containers.sh`

__________________________________________________________

Чтобы собрать образ:
docker build -t my-http-server .
Чтобы запустить контейнеры:
./start_servers.sh <порт_с_которого_начнется_итерация> <количество_серверов>

Чтобы удалить контейнеры: ./rm_containers.sh
Чтобы удалить папки: rm_server_folders.sh или sudo ./rm_server_folders.sh